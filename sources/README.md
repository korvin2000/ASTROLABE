# Immutable source archive

These four uploads are preserved byte for byte. They are historical inputs, **not** parallel authoritative specifications. Start from [the current architecture map](../SOTA-BEST-MIXED-AGENT.md). Do not load this archive by default.

The baseline is authoritative over A/B/C; corrections are identified in the [review](../REVIEW.md) and [content-only diff](../audit/semantic-changes.patch). Original inherited links to unprovided files are intentionally not rewritten. See [source availability](../SOURCE-REGISTER.md).
