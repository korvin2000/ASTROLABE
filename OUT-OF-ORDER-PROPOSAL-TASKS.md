# ASTROLABE — кандидаты на внеочередную аналитическую реализацию

**Обновлено 2026-09-20:** OOO-07 / P6.1.5 DONE, D-59; [протокол](audit/OUT-OF-ORDER-P6.1.5.md).
12 целевых тестов, независимый перебор 300 наборов, исправление по review, ABI и полная Windows/JDK 26
сборка пройдены. Eval 30 тестов выполнены; core 813/6 skips и provider-api 15 использованы UP-TO-DATE.
**65/181 DONE, 4 IN_PROGRESS, 112 TODO**; активного override нет. Следующий кандидат **OOO-08**.
OOO-01/02/03/05/07 выполнены. TODO §1.2 — источник статусов; родители и CI/Linux/live gates не закрыты.

Владелец хочет отдавать приоритет математике, статистике, сложным алгоритмам и аналитической проверке,
сохраняя компактный Kotlin-код, архитектуру проекта и возможность продолжать основной план.
Сложность должна вытекать из реальной задачи: дополнительные модели, оптимизаторы и инфраструктура
полезны только при наличии требования и проверяемого выигрыша.

Этот документ задаёт **выбор и порядок работы**, но не заменяет спецификации в `docs/`.
[TODO.md](TODO.md) остаётся единственным источником статусов исполнения и зависимостей;
[actual_state.md](actual_state.md) и [CONTINUE-TASK.md](CONTINUE-TASK.md) — его краткие проекции.
Образец завершённого внеочередного изменения: [протокол P2.1.1](audit/OUT-OF-ORDER-P2.1.1.md).
Порядок чтения для новой сессии: **§1 → §2 → выбранная карточка → §4–§6 → соответствующая строка §9**.
§2 — единственный источник порядка; §7 — индекс сопоставления IDs, а порядок размещения карточек не очередь.

## 1. Как начать в новой сессии

1. Прочитай `git status --short --branch`, `git log -5 --oneline`, три файла состояния и TODO §0–§3.
   Затем загрузи архитектурные инварианты по обычному resume-протоколу. Сохрани чужие изменения.
2. Если TODO §1.2 содержит ACTIVE-карточку внеочередной работы, продолжай **её точный checkpoint**.
   Для этой задачи загрузи раздел ниже и журнал `audit/OUT-OF-ORDER-<канонический-ID>.md`.
3. Если активной карточки нет и пользователь поручил реализацию/продолжение out-of-order,
   сначала проверь сохранённые частичные реализации: продолжи их прежний канонический ID/checkpoint.
   Иначе выбери первый ещё не выполненный допустимый кандидат **по убыванию сложности из §2: OOO-08**.
   Если он недопустим, сохрани конкретную причину в журнале отбора/карточке TODO §1.2 и проверь следующий ID.
   Высокая сложность сама по себе не повод нарушать выбранный порядок. Модуль `eval` уже создан P6.1.4;
   он содержит расчётное ядро, но не runner/frozen campaigns. OOO-01/02/03/05 уже выполнены.
   Поручение реализовать задачу достаточно для обычных обратимых шагов; повторное подтверждение каждого шага не требуется.
4. До кода выполни процедуру допуска из §4: проверь реальные зависимости и зарегистрируй самостоятельный
   вычислительный компонент либо возьми существующую задачу целиком, если все её зависимости уже готовы.
   Только после обновления ACTIVE-карточки и трёх файлов состояния начинай тесты и реализацию.
5. Если выделить полезный компонент без отсутствующих производителей нельзя, запиши конкретную причину
   и проверь следующего кандидата. Отсутствие зависимости не компенсируется заглушкой или вымышленным типом.
6. Завершив выбранный объём, закрой карточку и верни указатель на основной план. Следующего кандидата
   запускай лишь в рамках продолжающегося поручения на несколько внеочередных задач; иначе оставь его предложением.

Само наличие или чтение этого файла **не активирует** новую разработку. При обычном «продолжи основной план»
используй актуальную очередь TODO; при запросе только на планирование обновляй документы, не код.

### Проверенное состояние для новой сессии

- P2.1.1 уже DONE: `graph.RequirementGraph`, итеративные Tarjan/Kahn, `graph.Sizing`, расширение
  `contract.Increment`, доказательства и агрегированный ledger. Повторно реализовывать этот компонент не нужно.
- OOO-01/P2.3.4 DONE: [ContextCover](core/src/main/kotlin/io/astrolabe/context/ContextCover.kt), D-55,
  [журнал](audit/OUT-OF-ORDER-P2.3.4.md). OOO-03/P2.6.5 DONE:
  [CalibrationStats](core/src/main/kotlin/io/astrolabe/kb/CalibrationStats.kt), D-56,
  [журнал](audit/OUT-OF-ORDER-P2.6.5.md). Их API, тесты и интеграционные границы уже существуют.
- В `Sizing` есть `turns`, `continuations`, `rebuilds`, `filesTouched`; **`expectedFiles` принадлежит `Increment`**.
  Сбор и сохранение sizing по реальным завершениям ещё относятся к P2.1.4.
- Существуют `Tokens`, `Budget`, `Money`, `BillableUsage`, `TokenEstimator`, `ContractSlice`, Atlas и tier-0
  SymbolIndex. `Money` использует десятичную арифметику и явный признак неизвестной стоимости.
- Полного Compiler, постоянного Note store, ImportGraph и контроллера пока нет. `eval` содержит P6.1.4,
  но не runner/Accounting/frozen campaigns. [API guide](eval/README.md), [протокол](audit/OUT-OF-ORDER-P6.1.4.md).
  `Kb`/`EmptyKb` — контракт чтения, а не завершённый P2.6.
- P5.1.5 / OOO-05 DONE: `workspace.ScopeAlgebra`, D-58; [протокол](audit/OUT-OF-ORDER-P5.1.5.md),
  [API guide](core/README.md). Worktrees, ownership installation, leases/integration и S3 gates ещё не готовы.
- Счётчики: **65/181 DONE, 4 IN_PROGRESS, 112 TODO**; четыре невыполненных предложения ещё не добавлены в TODO.
- P6.1.5 / OOO-07 DONE: `eval.WorkloadSplit`, D-59; метаданные, validator, bounded exact search; runner/FX-47 остаются P6.1.1/P6.1.2.
  Родители P2.3.1/P2.6.4/P6.1.3/P5.1.1/P5.1.4 остаются TODO. Последний decision ID — D-59; следующий D-60
  проверяется на уникальность при активации, как и все предлагаемые task IDs из §7.
- Последняя полная сборка (checkpoint P5.1.5): Windows/JDK 26, core 813 и eval 18 тестов исполнены,
  0 failures/errors, 6 платформенных skips в core; provider-api UP-TO-DATE (15 green results).
  Это проверка данного checkpoint, не будущего diff.
- **Возврат в основной план:** P0.1.2 вместе с P0.6.1/P0.6.2/P0.6.4 → P1.8.2.
  CI/Linux остаются незакрыты; единичный FX-22 poll/terminal-status сбой сохранён в журнале P2.1.1.
  Все live gates — `UNMEASURED`. При новой сессии эти сведения необходимо сверить с актуальным TODO и кодом.

<a id="complexity-order"></a>

## 2. Невыполненные задачи: от самой сложной к более простой

**Оставшийся порядок по поручению владельца: OOO-08 → OOO-06 → OOO-09 → OOO-04.**
OOO-07 завершена после OOO-02/05. OOO-01/02/03/05/07 исключены из очереди.

Сложность — инженерная оценка **корректной реализации всего объявленного ядра и доказательности проверки**:
математическая/статистическая трудность, неоднозначность модели, крайние случаи и цена независимого oracle.
Асимптотика — один из факторов: стандартная экспоненциальная DP может быть проще корректной статистической
процедуры с зависимыми наблюдениями. Это не измеренные трудозатраты, contest rating или benchmark модели.

| Место | ID / возможный task ID | Ядро / родитель | Почему стоит на этом месте | Карточка |
|---|---|---|---|---|
| **1** | **OOO-08 → P4.5.5** | Оптимальный порядок DAG / P4.5.3 | Exact subset-DP с prerequisites, моделью switch cost, восстановлением оптимума и границами применимости; алгоритм более стандартен, чем у первых трёх | [§8.4](#84-ooo-08--оптимальный-порядок-малого-dag-при-стоимости-переключений) |
| **2** | **OOO-06 → P4.5.4** | Ожидаемая стоимость цепочек попыток / P4.5.1 | Условные вероятности, терминальные исходы, вся стоимость и uncertainty; при замороженной конечной модели обратный расчёт относительно компактен | [§8.2](#82-ooo-06--ожидаемая-стоимость-цепочек-retryreviewintegration) |
| **3** | **OOO-09 → P1.11.3** | Аналитика параллельных traces / P1.11.1 | Согласование ancestry и causality, clocks, intervals и exclusive/inclusive учёта; алгоритмическая база — sweep line и DAG traversal | [§8.5](#85-ooo-09--точная-аналитика-вложенных-и-параллельных-spans) |
| **4** | **OOO-04 → P3.2.7** | Impact/blast-radius / P3.2.2, потребитель P3.2.5 | BFS, пересечения, логарифмический риск и conservative fallback; меньше новых математических механизмов, основная трудность в честной обработке unknown | [OOO-04](#ooo-04--impact-достижимость-выбор-проверок-и-риск) |

Это порядок выбора **независимых компонентов**, не новая цепочка Deps. Перед началом каждого применяется §4.
OOO-08 остаётся ограниченным offline oracle, не автоматически включённым runtime optimizer.
Смена рейтинга, уменьшение acceptance или превращение ядра в другую задачу требуют явной записи причины;
сложную задачу делить на проверяемые подшаги, сохраняя весь заявленный Done.

## 3. Карточки кандидатов

### OOO-01 — dependency-aware greedy cover и бюджетная арифметика

**Выполнено:** P2.3.4, D-55; карточка оставлена как контекст интеграции. В очередь §2 не входит.

**Источники:** [compiler §6.1](docs/context/compiler.md#sec-6-1), TODO P2.3.1, D-18, D-06, D-52;
[context layout](docs/runtime/context-layout.md), [continuity §6.5](docs/context/continuity.md#sec-6-5).
Читайте companion-разделы только при пересечении соответствующей границы.

**Ранний объём:** чистая функция над идентифицированными единицами контекста: стоимость, зависимости,
обязательность, класс приоритета и явно переданные признаки полезности. Это проекция для расчёта,
а не альтернативная модель `Note`, хранилище KB или новый `Compiler` вместо P1.9.3.
Результат содержит выбранные ID, проверяемую арифметику и причины пропусков/невозможности размещения.

```text
bundle(v) = {v} ∪ transitive_dependencies(v)
additional(v, selected) = bundle(v) \ selected
marginal_tokens(v, selected) = Σ tokens(x), x ∈ additional(v, selected)
available = C_profile·α − S − R − pinned_history − retained_protocol − reserves
```

**Требования и решения до кода:**

- Mandatory включает своё полное замыкание. Переполнение обязательной части возвращает capacity/rescope,
  сохраняя её состав и причины; обязательные правила и acceptance не сокращаются для достижения fit.
- Общая зависимость оплачивается один раз. Уже включённые `[S]/[R]` учитываются в общей смете, но не
  дублируются в `[K]`. Источник стоимости и оценочность величин сохраняются.
- Приоритеты следуют §6.1. Спецификация не задаёт универсальный `gain`: определение признаков,
  выбор отношения gain/marginal-cost, округление и tie-break необходимо обосновать и записать в `D-nn`.
  Отношение — возможная реализация эвристики, а не новая нормативная формула проекта.
- Определи обработку циклов, отсутствующей зависимости, нулевой стоимости и нулевого прироста.
  Неразрешённая обязательная зависимость всегда препятствует успешному результату.
- Сложность наивного и оптимизированного вариантов укажи явно. Если используется heap, докажи
  корректность обновлений: включение общей зависимости меняет marginal cost других кандидатов.
  Старый ключ heap не обязан оставаться допустимой границей. Сначала достаточно корректного пересчёта.
- Счётчики и суммы защищены от overflow; отрицательные/нечисловые значения отвергаются. Политика
  округления не превращает превышение бюджета в fit. Неизвестная effective history не становится нулём.

**Проверка:** ручной diamond-граф с общей зависимостью; mandatory на точной границе и на один токен выше;
циклы/пропуски; перестановки входа; большие счётчики; детерминированные случайные DAG.
Независимый медленный эталон проверяет выбранную greedy-политику. Полный перебор маленьких наборов
проверяет допустимость и измеряет отставание от оптимума; равенство greedy и optimum не требуется.
Гарантии приближения и монотонного улучшения качества с бюджетом допустимы только при доказанных предпосылках.

**Граница DONE ядра:** валидный детерминированный выбор/отказ, обязательность и все стоимости проверяемы,
тесты и ABI/build проходят. **Остаётся родителю:** источники Note/CarryForward/seeds, рендер всех сериализованных
частей, Manifest P2.3.2, полный coverage и dispatch Admission P2.3.3. Planning estimate не даёт разрешения на вызов модели.
Вероятные файлы: `core/.../context/` и соответствующие тесты; новые библиотеки не нужны.

### OOO-02 — scorecard и парная статистическая оценка promotion

**Выполнено:** P6.1.4, D-57; [протокол](audit/OUT-OF-ORDER-P6.1.4.md), [API](eval/README.md).
Карточка — контекст интеграции; в очередь §2 не входит. Родитель P6.1.3 остаётся TODO.

**Источники:** [evaluation §§19.2, 19.4, 19.6](docs/evaluation/method.md),
[costs](docs/economics/costs.md), TODO P6.1.3, D-28/D-48;
существующий [Money/BillableUsage](provider-api/src/main/kotlin/io/astrolabe/provider/Usage.kt).

**Ранний объём:** вычисление на явно переданной таблице trials и замороженной политике оценки.
Разделить на проверяемые части: A — арифметика/eligibility; B — paired uncertainty; C — составной verdict
и синтетические эксперименты. Наличие A не закрывает B/C. Никаких live-запросов и автоматического включения feature flags.

```text
Q = 100·Σ_s w_s·accepted_s / total_s
Cost_s = total_online_cost_s / accepted_s
E_s = 100·clamp((Cost_high_s − Cost_s)/(Cost_high_s − Cost_low_s), 0, 1)
E = Σ_s w_s·E_s
eligible_score = 0.60·Q + 0.40·E        только для eligible конфигураций
```

**Требования и решения до кода:**

- Один trial — полная задача с разрешёнными внутренними retry; helpers, review, неуспешные попытки и
  integration входят в стоимость, а не превращаются в дополнительные успешные trials.
- Веса неотрицательны, сумма равна единице с объявленным правилом численной точности; каждый взвешенный
  stratum представлен. `0 ≤ Cost_low < Cost_high`, одна валюта, frozen pilot thresholds.
  При нуле accepted стоимость на accepted неопределена, `E_s=0`, quality floor не пройден.
- `Money.unknown`, неполный billing, нарушения инвариантов и недостаточные данные сохраняют свои причины.
  Unknown не становится нулём; высокий aggregate score не перекрывает потерю сложной страты или safety floor.
  Online и one-off investment учитываются отдельно; repayment-volume неопределён при отсутствии положительной экономии.
- Определи estimand, ключ пары baseline/candidate и независимую единицу. Повторы одной задачи не являются
  независимыми задачами; кластеризация по repository/task и веса страт должны соответствовать дизайну эксперимента.
  Несопоставимые/неполные пары нельзя молча выбрасывать для улучшения результата.
- До просмотра результатов зафиксируй метод односторонней границы, уровень confidence, обработку малого
  числа кластеров, multiple-selection и вырожденных данных. Парный cluster bootstrap — кандидат, не автоматически
  корректное решение для любых данных. Метод проверь по первичным статистическим источникам и запиши ссылки/предпосылки.
- Нормативный margin: **−2 процентных пункта = −0.02** при долях в `[0,1]`, отдельно overall и complex.
  Уровень confidence, число реплик, PRNG/seed и минимально достаточные данные спецификацией не определены:
  зафиксируй их как явные параметры/`D-nn`, не подбирай по желаемому verdict.
- Inconclusive сохраняет baseline. Синтетический успех проверяет программу, а не утверждает реальную promotion.
  Даже при математически положительном выводе отсутствие frozen manifests/целостности evaluation не разрешает adoption.

**Проверка:** ручные таблицы для арифметики и границ; нулевые denominators/unknown billing/валюта;
Simpson-подобный пример, где общий выигрыш скрывает потерю сложной страты; парные повторы внутри кластеров;
один кластер/вырожденная выборка; фиксированный seed и перестановки строк. Независимая эталонная реализация
и заранее описанные симуляции проверяют bias/coverage и частоту ложного promotion в подходящих моделях данных.
Совпадение двух реализаций bootstrap само по себе не доказывает статистическую состоятельность.

**Граница DONE ядра:** численный отчёт с provenance и объяснимым verdict, валидация выбранного статистического
метода, синтетические тесты, ABI/build. **Остаётся родителю:** runner P6.1.1, frozen campaigns P6.1.2,
данные Accounting P1.11.2, evaluation-integrity checks, отчёты P6.3.1; live campaigns относятся к P7.
Размещение: будущий `eval/src/.../io/astrolabe/eval/`. Создание минимального Gradle-модуля допустимо только
при начале зарегистрированной задачи; оно не означает готовности P6.1.1. `core` не зависит от `eval`.

### OOO-03 — CalibrationStats на истории инкрементов

**Выполнено:** P2.6.5, D-56; карточка оставлена как контекст интеграции. В очередь §2 не входит.

**Источники:** [continuity §6.7](docs/context/continuity.md#sec-6-7), TODO P2.6.4/P2.1.4, D-42;
[Sizing](core/src/main/kotlin/io/astrolabe/graph/Planning.kt),
[Increment](core/src/main/kotlin/io/astrolabe/contract/Contract.kt).

**Ранний объём:** чистая агрегация переданных наблюдений и функция диагностического предупреждения.
Источник одной строки: идентичность наблюдения, `Increment.expectedFiles`, `Increment.sizing`, subsystem,
outcome/завершённость, provenance и версия политики. При отсутствии истории результат явно отсутствует.

```text
overrun_i = (continuations_i > 0) OR (rebuilds_i > 0)
overrun_rate(group) = count(overrun_i) / count(eligible observations in group)
turns_median = median(turns_i)
ratio_i = filesTouched_i / expectedFiles_i       определено только при expectedFiles_i > 0
mean_ratio = mean(defined ratio_i)              не ratio-of-sums
warning = overrun_rate(expected_files_band) > 0.50
```

**Требования и решения до кода:**

- Зафиксируй границы bands и их включённость, правило subsystem, единицу наблюдения и разделение
  разных policy/harness versions. Не выводи «измеренную» статистику из model-written CAL текста.
- `expectedFiles=0` не допускает деления; такие строки отдельно учитываются в количестве неопределённых ratios,
  но не исчезают автоматически из остальных статистик. Пустая группа не имеет rate=0.
- Для чётного числа значений медиана — середина двух центральных с арифметикой без overflow.
  Накопление ratios устойчиво к масштабу; результат сопровождается знаменателями и количеством исключений.
- Continuation одного инкремента не создаёт ещё одно независимое наблюдение. Повторная доставка checkpoint
  не удваивает выборку; конфликтующий duplicate ID требует явного решения, а не last-write-wins по порядку строк.
- Опиши обработку cancelled, failed и ещё незавершённых наблюдений, чтобы не получать скрытый survivorship bias.
  Цензурирование/исключение имеет причину и отдельный count. Half-life/window, если нужны, объявляются отдельно
  от исходных статистик; формула decay не выдумывается как уже принятая спецификацией.
- Число `0.50` взято из спецификации, порог строгий. Новые эмпирические поправки, routing demotion и Bayesian
  модели в этот объём не входят без отдельного обоснования и проверки пользы.

**Проверка:** нечётная/чётная медиана, пустая история, нулевой expectedFiles, большие счётчики, ровно 50%
и чуть выше, несбалансированные группы, порядок строк, дубликаты/конфликты и terminal vs censored.
Например, ratios `1/1` и `9/3` дают mean=2, тогда как ratio-of-sums=2.5 — эти показатели нельзя путать.

**Граница DONE ядра:** детерминированные статистики с корректными знаменателями и provenance, предупреждение
как чистый результат, тесты/ABI/build. **Остаётся родителю:** реальный сбор P2.1.4, SQLite-хранение P2.6,
событие Warning контроллера, ≤150-token плановый блок и его optional gate. Самостоятельная функция предупреждения
не считается отправленным событием. CAL injection остаётся выключенным до своего ablation; сбор статистики не отключается.
Размещение: `core/.../kb/` согласно владению P2.6; не вводить параллельный Sizing или новую DB ради расчёта.

### OOO-04 — Impact: достижимость, выбор проверок и риск

**Источники:** [navigation §§7.3–7.4](docs/repository/navigation.md#sec-7-3), TODO P3.2.1–P3.2.6;
[scheduler](docs/verification/scheduler.md),
[tier-0 SymbolIndex](core/src/main/kotlin/io/astrolabe/atlas/SymbolIndex.kt).

**Ранний объём:** анализ переданного immutable graph snapshot, edit set, test/acceptance closures и anchored
contract projections. Реальное извлечение/разрешение импортов и загрузка KB остаются у своих производителей.

```text
blast(E) = E ∪ reverse_import_reachability(E)
affected_tests = tests_in_blast ∪ naming_edges ∪ {acceptance checks whose closure intersects blast}
contracts_touched = {CON/ADR projections whose anchors intersect E}
risk(E) = Σ_hunks changed_lines · (1 + log2(1 + fanin(enclosing_symbol)))
```

**Требования и решения до кода:**

- Направление ребра формализовано; BFS reverse adjacency посещает вершину один раз. Циклы импортов допустимы:
  они не означают цикл prerequisite-инкрементов. Не переносить сюда запрет циклов RequirementGraph.
- Идентичность файла включает workspace/package; одинаковые имена из разных модулей не сливаются.
  `tier`, `complete`, unresolved dependencies и охват сохраняются до результата.
- Unknown closure и неполный граф приводят к явному требованию более широкой проверки — package suite,
  а при неизвестном package к ещё более консервативной области. Вычислительный результат не исполняет команды.
  Unknown fan-in не равен доказанному нулю: численная оценка и неопределённость представлены раздельно.
- Acceptance связывается **пересечением** closure с blast, не только наличием имени изменённого файла в команде.
  Контрактные anchors пересекаются с E по спецификации; эти две области не подменяют друг друга.
- Определи `changed_lines` для add/delete и перекрывающихся hunks до реализации; величина неотрицательна,
  изменения не взаимоуничтожаются через signed net delta. `θ=40` берётся из Config/Defaults, условие `risk > θ`.

**Проверка:** независимый reachability oracle на маленьких графах, diamond/cycle/disconnected components,
одинаковые имена в разных packages, unknown closure, runtime-only dependency, acceptance, пересекающий blast
только транзитивно. Ручной пример: `10·(1+log2(4)) + 5·(1+log2(2)) = 40`; на этой границе порог не превышен.
Это новый тестовый пример, а не приписанный спецификации benchmark. Добавление хунка `(1, fanin=0)` даёт 41.
Покрыть вычислительные части FX-37 и FX-54; runtime-fixture не объявлять выполненной без соответствующего пути.

**Граница DONE ядра:** корректные closures/intersections/risk и явный conservative fallback, детерминизм,
оценка O(V+E) для BFS и отдельно стоимости joins, тесты/ABI/build. **Остаётся родителям:** P3.2.1 parsers/resolution,
P3.2.3 tools, P3.2.4 nudges, P3.2.5 scheduler/verify binding, P3.2.6 pre-scan; KB anchors и routing имеют своих владельцев.
Размещение: `core/.../atlas/`; не создавать эвристический language server вместо математического ядра.

## 4. Как выделять ядро без обхода зависимостей

Предложения относятся к самостоятельным ядрам; **готовность полного родителя проверяется отдельно**.
В отличие от полностью выполненного P2.1.1, ранняя реализация здесь обычно закрывает только новый компонент.
Успешные тесты ядра не меняют исходный `Done` родителя.

1. Составь таблицу «потребляемый тип → существующий файл → producer task → статус». Посмотри явные `Deps`
   и правило неявных зависимостей внутри work package. Отдельно перечисли будущих потребителей/интеграции.
2. Если полный родитель dependency-ready и выбран целиком, используй его существующий ID и полный `Build/Done`.
   Иначе добавь **самостоятельную каноническую задачу вычислительного ядра** в конец соответствующего пакета TODO,
   с явными готовыми `Deps`, собственными `Build/Done` и ссылкой на ID предложения. Существующие ID не перенумеровываются.
3. Возможные свободные номера семи новых ядер указаны в §7. **P2.3.4 и P2.6.5 уже заняты выполненными
   OOO-01/03**. Остальные номера — подсказки, не резервирование и не существующие задачи. Перед добавлением
   проверь уникальность; если номер занят новой работой, выбери следующий свободный в том же пакете,
   запиши mapping в журнал/§7/ACTIVE-карточку и сохраняй его при resume.
4. У новой задачи обязательны **явные Deps**: иначе правило «все предыдущие задачи пакета» снова заблокирует ядро.
   Ориентиры: OOO-01 — готовые budget/token contracts; OOO-02 — money/identity contracts и новый локальный набор
   evaluation input records; OOO-03 — P1.1.1 + P2.1.1; OOO-04 — существующие identity/atlas/closure contracts.
   Конкретный список выводится из реальных импортов и владельцев; ссылки на предполагаемые классы не считаются готовностью.
5. В TODO §3 добавь следующий свободный `D-nn`: причина выделения, точная граница, владение типами, отсутствующие
   интеграции и назначенные потребители. Это обычное исполнение разрешённого out-of-order-поручения, а не повод
   повторно спрашивать разрешение на обратимую декомпозицию. Изменение авторизованного acceptance требует отдельного основания.
6. В §2.4 зарегистрируй **конкретные типы ядра** за новой задачей. `Note`, `Compiler`, `Manifest`, `ImportGraph`
   и другие отсутствующие подсистемы сохраняют своих производителей. Чистый input projection имеет самостоятельный
   узкий контракт; он не должен имитировать отсутствующий runtime-объект.
7. У родителя добавь ссылку «использовать готовое ядро после его DONE» и список оставшегося `Build/Done`.
   Если добавляешь явный `Deps` родителю, сохрани все его прежние явные **и неявные** prerequisites, затем добавь ядро.
   Проверь отсутствие цикла: ядро не зависит от родителя. Возникающий цикл требует честной дополнительной декомпозиции,
   а не удаления неудобной зависимости или снижения требований.
8. Пересчитай план по заголовкам. Например, добавление одного ядра IN_PROGRESS к checkpoint `f6a4feb` даёт
   **179 задач: 62 DONE, 5 IN_PROGRESS, 112 TODO**; после его закрытия — **63/179 DONE, 4 IN_PROGRESS, 112 TODO**.
   Родитель остаётся TODO, если его собственная реализация не начиналась. Это пример арифметики, не будущая константа.

Если полезный самостоятельный API требует отсутствующих producers, запиши эту причину недопуска.
Для большого, но допустимого ядра используй подшаги и IN_PROGRESS checkpoint; размер работы сам по себе
не отменяет порядок сложности и не уменьшает Done. Переносить целиком controller/KB/provider/evaluator
framework только ради возможности поставить DONE не следует.

## 5. Рабочий цикл и критерии качества

Работай одним вычислительным компонентом за раз. До реализации запиши математическую модель, единицы,
предпосылки, политику неизвестных данных, инварианты, ожидаемую сложность и тестовый oracle.
Непредписанные спецификацией коэффициенты/статистические решения помечай как новые решения, а не как существующие defaults.

Последовательность: контракт и минимальный падающий тест → малая реализация → целевые проверки →
проверка краёв/случайных примеров → review → ABI → полная сборка → документация → checkpoint-коммит.
Если задача велика, используй маленькие проверяемые подшаги внутри неё; частичный коммит не означает DONE всей задачи.

| Измерение | Условие принятия |
|---|---|
| Математическая корректность | Формулы, единицы, знаменатели и границы совпадают со спецификацией; assumptions перечислены; ручные и независимые oracle-проверки проходят |
| Статистическая честность | Единица анализа, pairing/strata/clusters и uncertainty согласованы; недостаток данных не выдаётся за доказательство; policy frozen до оценки |
| Алгоритмы | Терминация, детерминизм и заявленная сложность обоснованы; большие/циклические/переставленные входы проверены; оптимизация сравнена с простым эталоном |
| Integrity | Mandatory acceptance, authority, provenance, currency и неопределённость сохранены; входные коллекции защищены на границах; чужие изменения сохранены |
| Чистота кода | Владение по TODO §2.3, компактные domain-типы, существующие библиотеки/stdlib; новые абстракции и зависимости имеют конкретную необходимость |
| Интеграция | Реальные потребители перечислены; источники ещё отсутствующих данных названы; kernel DONE не выдаётся за runtime DONE |
| Проверки | Тесты не отключены, thresholds не ослаблены; соответствующие ABI и build проверки выполнены; ограничения по платформе названы |
| Resume | По сохранённым файлам можно определить выполненное, оставшееся, следующий шаг, доказательства и точку возврата без истории чата |

Ошибки и неопределённость не усредняются в общий «балл качества». Любой нарушенный обязательный критерий
препятствует DONE. Оценка скорости/памяти содержит размер данных, окружение и метод измерения; произвольный
«олимпиадный» performance threshold не заменяет требования проекта. Исследовательские сравнения не включают функции в production.

Проверки выполняются pinned toolchain репозитория. Примеры PowerShell для текущей машины:

```powershell
$env:JAVA_HOME = 'C:\Users\user\.gradle\jdks\eclipse_adoptium-26-amd64-windows.2'
$env:GRADLE_USER_HOME = 'C:\Users\user\.gradle'
# Для первой задачи OOO-02, после регистрации P6.1.4 и создания eval по §9.2:
./gradlew.bat :eval:test --tests 'io.astrolabe.eval.*' --offline --console=plain --no-configuration-cache
./gradlew.bat :eval:updateKotlinAbi --offline --console=plain --no-configuration-cache
# Для core-ядер: :core:test с пакетом из §9.1, затем :core:updateKotlinAbi при изменении API.
# updateKotlinAbi — отдельная invocation только для модулей с изменённым public API.
./gradlew.bat build --offline --console=plain --no-configuration-cache
```

Путь JDK машинный: проверь его в новой среде. Для нового `eval` обнови/проверь его ABI аналогично convention plugin.
После успешных проверок повторный запуск нужен при новых изменениях или конкретной нерешённой проблеме.
Отдельно фиксируй выполненные тесты, UP-TO-DATE результаты, skips и отсутствующую Linux/CI-валидацию.
Filtered run заменяет XML-отчёт core; сохрани сводку предыдущего запуска до его перезаписи.

## 6. Обновление документации и сохранение прогресса

| Когда | TODO.md — источник статуса | actual_state.md / CONTINUE-TASK.md | Журнал и этот документ |
|---|---|---|---|
| До первого изменения кода | Создать/выбрать каноническую задачу, explicit Deps и producer mapping; IN_PROGRESS; ACTIVE-карточка в §1.2; текущие счётчики §1 | В начало — точный ID, scope, checkpoint, возврат; обычная очередь временно уступает выбранной задаче | Создать журнал; записать mapping OOO → task, baseline, решения и проверки допуска |
| После каждого проверенного подшага | Датированный `Log:`: сделано, результат проверок, оставшееся; статус соответствует факту | Коротко обновить последний checkpoint и следующую команду/подзадачу | Записать команды, seed/версии входов, результаты/ошибки, анализ; не хранить приватные рассуждения или секреты |
| Прерывание сессии | Сохранить IN_PROGRESS/реальный blocker; ACTIVE остаётся, пока поручение продолжается; DONE не ставить | Назвать точные файлы, незавершённый шаг и необходимые проверки; отдельной строкой состояние сборки | Перечислить незакоммиченное; сохранить воспроизводимый контрпример и известные ограничения |
| Ядро завершено | DONE только у ядра; родителю — выполненный компонент и оставшаяся интеграция; §1.2 COMPLETE; пересчёт §1 | Убрать временный override, восстановить актуальную основную очередь, добавить «не реализовывать повторно» | Итоговый протокол и checkpoint-коммит; mapping остаётся постоянным, следующий кандидат не активируется молча |

Новую ACTIVE-карточку помещай в начало TODO §1.2. Историю завершённых P2.1.1/P2.3.4/P2.6.5 сохраняй ниже;
их Log, D-54–D-56 и результаты не перезаписываются карточкой следующего кандидата.
Статусы в этом файле — только датированный индекс предложений, ссылающийся на TODO, а не конкурирующий tracker.

Явное новое поручение владельца вернуться к основному плану имеет приоритет над ACTIVE-карточкой.
Если ядро при этом не завершено, сохрани его IN_PROGRESS и checkpoint, а в TODO §0.1/§1 и обоих handoff-файлах
явно укажи выбранную основную задачу и временно отложенный внеочередной ID. Выбор по общему списку IN_PROGRESS
должен учитывать это распоряжение, чтобы сразу не запустить отложенную работу снова. При следующем поручении
возобновить out-of-order восстанови прежнюю карточку и продолжи с checkpoint, сохранив достигнутый прогресс.
Приостановка по распоряжению владельца не является техническим blocker или выполнением задачи.

Минимальная карточка активной работы в TODO §1.2:

```text
State: ACTIVE
Proposal: OOO-…  | Canonical task: P…  | Parent: P…
Authority: запрос владельца на реализацию out-of-order, дата/краткий смысл
Baseline: branch + commit; исходные dirty paths
Scope / excluded integration: конкретный Build/Done ядра и владельцы оставшегося
Dependencies / producer evidence: реальные task IDs и файлы
Decision records: D-…
Checkpoint: последний проверенный подшаг; коммит либо точные незакоммиченные файлы
Validation: команды, результаты, платформа, воспроизводимый failure/seed при наличии
Next action: конкретный следующий шаг, не «продолжить работу»
Return point: актуальная основная очередь; на исходном checkpoint — P0 validation → P1.8.2
Journal: audit/OUT-OF-ORDER-<канонический-ID>.md
```

Перед checkpoint-коммитом проверь `git diff --check`, diff и согласованность статусов/ссылок/чисел во всех
трёх файлах. Коммит включает соответствующие код, тесты, ABI и handoff; несвязанные пользовательские правки
не включаются. Push и публикация остаются отдельными действиями по полномочиям текущей сессии.
Если тест/build красный, сохрани диагностику и честный checkpoint без DONE; не исправляй статистику отчёта пропуском теста.

При возвращении к родителю в основном плане сначала найди его Log и запись producer table, открой код и тесты
готового ядра, затем реализуй **оставшиеся** интеграции. Менять DONE на TODO из-за отсутствия будущего подключения
или закрывать весь родитель по unit-тестам ядра одинаково неверно. Изменение уже готового контракта оформляется
явным новым изменением с проверкой потребителей и ABI. Phase completion, platform validation и PROMOTED
учитываются независимо от количества завершённых компонентов.

## 7. Индекс предложений (обновлён 2026-09-20)

| Proposal | Состояние | Каноническая задача ядра | Следующее действие |
|---|---|---|---|
| OOO-02 | DONE (TODO §1.2, 2026-09-20) | P6.1.4, D-57 | Использовать готовое ядро при интеграции P6.1.3; [протокол](audit/OUT-OF-ORDER-P6.1.4.md) |
| OOO-05 | DONE (TODO §1.2, 2026-09-20) | P5.1.5, D-58 | Использовать готовое ядро при интеграции P5.1.1/P5.1.4; [протокол](audit/OUT-OF-ORDER-P5.1.5.md) |
| OOO-07 | DONE (TODO §1.2) | P6.1.5, D-59 | Модель зафиксирована; [протокол](audit/OUT-OF-ORDER-P6.1.5.md); §8.3 |
| OOO-08 | PROPOSED | ещё не создана; кандидат P4.5.5 | Ограничить модель стоимости переключений и размер exact oracle; §8.4 |
| OOO-06 | PROPOSED | ещё не создана; кандидат P4.5.4 | Зафиксировать модель цепочек попыток и происхождение вероятностей; §8.2 |
| OOO-09 | PROPOSED | ещё не создана; кандидат P1.11.3 | Определить exclusive spans, clock domain и causal DAG; §8.5 |
| OOO-04 | PROPOSED | ещё не создана; кандидат P3.2.7 | Определить graph input, changed_lines и распространение unknown |
| OOO-01 | KERNEL DONE, вне очереди | P2.3.4 | Использовать в P2.3.1; renderer/Manifest/admission остаются у родителей; [журнал](audit/OUT-OF-ORDER-P2.3.4.md) |
| OOO-03 | KERNEL DONE, вне очереди | P2.6.5 | Использовать в P2.6.4; сбор/persistence/Warning/optional CAL остаются у родителей; [журнал](audit/OUT-OF-ORDER-P2.6.5.md) |

Индекс сверяется с TODO при каждом checkpoint. Новые строки PROPOSED не увеличивают счётчики;
после регистрации рядом с OOO ID сохраняется фактический канонический ID. Поиск и сортировка меняют
только документацию: предыдущие build/ABI-свидетельства относятся к реализованному коду, не к этим правкам.

## 8. Повторный отбор: пять новых кандидатов на checkpoint 2cb9372

**Дата:** 2026-09-20. Просмотрены все оставшиеся TODO-заголовки, профильные спецификации и имеющиеся
реализации. Working tree при начале поиска чистый. Запрос «поискать кандидатов out-of-scope» трактуется
в контексте этой работы как внеочередная реализация внутри архитектуры ASTROLABE, а не расширение P7.
Ни один новый компонент здесь не активирован. Счётчики остаются **62/178 DONE, 4 IN_PROGRESS, 112 TODO**.

**Карточки ниже сохранены с исходными номерами разделов для стабильности ссылок.** Прежний рейтинг
полезности заменён очередью сложности в §2; начинать с OOO-05 только из-за положения его карточки здесь
не следует. Методы — предложения для будущего `D-nn`, не уже утверждённые defaults или обещание выигрыша.
§4–§6 задают протокол допуска/завершения, §9 — последовательность проверяемых подшагов.

### 8.1 OOO-05 — пересечение write scopes с конструктивным доказательством

**Выполнено:** P5.1.5, D-58; [протокол](audit/OUT-OF-ORDER-P5.1.5.md), [API guide](core/README.md).
Карточка сохранена как контекст интеграции; в очередь §2 не входит. S3/родители остаются незавершёнными.

**Основание:** [delegation §10.4](docs/operations/delegation.md#sec-10-4), TODO P5.1.1/P5.1.4,
`Scope` и [PathPattern.kt](core/src/main/kotlin/io/astrolabe/workspace/PathPattern.kt).
`RequirementGraph.validate` уже проверяет конкретные ownership paths относительно scope; оно не
доказывает непересечение двух языков шаблонов. Новый компонент не должен повторять эту проверку.

**Полезный результат:** для двух объявленных областей вернуть `Disjoint`, `Overlap(witnessPath)` или
`Unknown(reason)`. Для проверки вложенности — путь, разрешённый child scope, но не parent scope.
Входы уже приводятся к одной целевой namespace интеграции: разные child-workspace ID сами по себе
не доказывают отсутствие конфликта при публикации в общий destination.

```text
L(scope) = union(L(writePatterns)) \ union(L(protectedPatterns))
overlap(A,B) ⇔ L(A) ∩ L(B) ∩ CanonicalPaths ≠ ∅
subset(A,B)  ⇔ (L(A) \ L(B)) ∩ CanonicalPaths = ∅
```

- Сохранить точную семантику `dir/`, bare filename, slashed prefix, `*`, `?`, `**`, `**/` из PathPattern;
  это не стандартный shell glob. Например, `src/**` и `**/billing.kt` пересекаются на `src/billing.kt`.
- Кандидат метода: перевод в автоматы и поиск по произведению, с восстановлением кратчайшего свидетеля.
  [Учебный первоисточник Cornell](https://www.cs.cornell.edu/courses/cs212/1998sp/psets/ps4.html)
  описывает product construction; применение к ASTROLABE и ограничения ниже — наше проектное предложение.
- До кода определить допустимый алфавит/классы символов, канонические сегменты, Unicode и tie-break свидетеля.
  Witness повторно проверяется существующим `PathPattern.matches`. Символьный расчёт не разрешает symlink,
  case alias или filesystem race: реальные пути и полномочия по-прежнему проверяет WorkspacePath/ScopeGuard.
- Для уже построенных DFA размеров a,b и c классов символов достижимость занимает O(a·b·c).
  Это **не** полиномиальная гарантия по длине всех исходных globs: детерминизация, исключения и произведения
  могут разрастаться экспоненциально. Лимит состояний даёт Unknown, никогда ложное Disjoint.
- Проверки: независимый matcher на малом алфавите, исчерпывающий перебор коротких путей, deep patterns,
  перестановка union/exclusion, короткий witness, `**/x` на корне, protected exception, неизвестные формы.
  Ограниченный перебор строк не является доказательством пустоты бесконечного языка: нужен полный обход автомата.

**Условие допуска:** документация требует disjoint ownership, но не требует автоматы. Если реальный
потребитель фиксирует конечный target inventory и запрещает новые пути, достаточно пересечения множеств;
в таком режиме этот кандидат откладывается. Анализ языка оправдан для открытых write scopes, включая
ещё не существующие файлы. Он не включает S3 и не доказывает семантическую независимость изменений.

**Владение/Deps:** предлагаемый P5.1.5, `core/workspace`; реальные входы — P1.1.1 Scope, P1.7.8 PathPattern,
P1.2.6 WorkspacePath и P0.2.1 namespace identities при их потреблении (все DONE). Worktree lifecycle, ownership installation, leases, интеграция,
S3 gates остаются у P5.1.1/P5.1.3/P5.1.4. Названия будущего API регистрируются при активации.

### 8.2 OOO-06 — ожидаемая стоимость цепочек retry/review/integration

**Основание:** [routing §§11.2–11.3](docs/operations/routing.md#sec-11-2), TODO P4.5.1/P4.5.2, D-34/D-35,
[Money, PriceTable, BillableUsage](provider-api/src/main/kotlin/io/astrolabe/provider/Usage.kt).
Сейчас есть денежные типы, Profile и Tier; готового Router/оценки условных исходов нет.

**Ранний объём:** оценить явно переданную конечную модель попыток для нескольких фиксированных политик,
сохранив ожидаемую полную стоимость, вероятность complete acceptance, терминальные причины и provenance.
Вероятности и стоимости ветвей поставляет вызывающий код; компонент их не угадывает и не обучает.

```text
V_h(s) = c(s) + Σ_t p(t|s) · V_(h−1)(t)
A_h(s) = Σ_t p(t|s) · A_(h−1)(t)
```

Терминальная стоимость/acceptance задаются явно; предел h согласован с реальным attempts budget.
Это обратный расчёт фиксированных политик, а не новый адаптивный control loop. Для такой постановки
применима идея finite-horizon DP из [MIT/Bertsekas](https://ocw.mit.edu/courses/6-231-dynamic-programming-and-stochastic-control-fall-2015/304cf17d604e774625dae63810777264_MIT6_231F15_Lec1.pdf).

- Включать стоимость провалившихся попыток, helpers, review и integration. Простой synthetic пример:
  первая попытка стоит 1, успешна с вероятностью 0.5, иначе обязательный fallback стоит 8 и успешен;
  ожидаемый расход 5, тогда как однократный вариант за 4 с успехом 1 дешевле при одинаковом acceptance.
- Ветки failed/blocked/cancelled не исчезают и не перенормируются. Вероятности в [0,1], сумма 1 по
  заранее определённой точности. Одна валюта; неизвестная достижимая стоимость/вероятность → Unknown.
- Граничное условие исчерпания попыток не равно успеху. Conditional failure после предыдущего провала
  нельзя получить из общей `Profile.stratumOutcomes` делением accepted/trials и предположением независимости.
- Eligibility, risk/never-below floors и conservative affordability проверяются отдельно, до выбора
  минимального expected total cost. Математическое ожидание не заменяет резерв на реальный dispatch.
  `E[cost]/P(accepted)` может быть отдельной диагностикой; это не молчаливая замена нормативной цели router.
- Зафиксировать decimal/rational arithmetic, округление и propagation uncertainty. Интервалы вероятностей
  можно отложить; нельзя выдавать point estimate за доверительную границу или предполагать независимость ветвей.
- При H шагах и графе V/E: O(H·(V+E)) арифметических операций для каждой фиксированной политики; порядок
  и битовая сложность точных чисел оговариваются отдельно. Проверка — полный перебор всех малых траекторий,
  p=0/1, исчерпание бюджета, неизвестная цена, смена валюты, перестановки и пример «дешёвый старт дороже».

**Владение/Deps:** предлагаемый P4.5.4, `core/route`; P0.3.3 Money/Profile и P0.2.2 Budget — DONE;
P0.1.3, если API потребляет существующий Tier/Config. Новая узкая scenario projection принадлежит ядру.
Реальные outcome estimates, risk inputs, policy calibration и вызовы controller остаются P4.5.1/P4.5.2.
Политика automatic demotion остаётся OFF; положительный synthetic расчёт не разрешает смену профиля.

### 8.3 OOO-07 — разбиение workload с группами, стратами и ограничениями времени

**Выполнено:** P6.1.5, D-59; [протокол](audit/OUT-OF-ORDER-P6.1.5.md), [API](eval/README.md).

**Основание:** [evaluation §19.2](docs/evaluation/method.md#sec-19-2), TODO P6.1.2, FX-47.
Декларация split по repository **или** task-family/time уже есть; нового обязательного объединения всех
трёх критериев спецификация не вводит. Конкретная политика разбиения выбирается и замораживается явно.

**Ранний объём:** принять immutable таблицу идентичностей задач и разрешённую политику must-link/time;
проверить существующее разбиение и, при запросе, построить допустимое development/selection/final-разбиение.
Все повторы одной задачи остаются вместе. Только метаданные дизайна, без результатов candidate на final set.

- Сначала объединить must-link связи в неделимые компоненты (DSU/обход графа), затем распределять компоненты,
  сохраняя страты и объявленные квоты. Независимость определяется sampling design, не номерами строк.
- Условие ≥ половины **веса workload** на сложных классах не подменяется половиной числа строк. Нужны
  явные denominators, веса, counts по partition/stratum и причины отсутствующих/неразмещённых групп.
- Hybrid policy может дать транзитивный конфликт: A связан с B по repo, B с C по family; при объявленных
  must-link связях все три неделимы. Если компонент не помещается, нельзя разрезать его ради красивого баланса.
- Time cutoffs и разрешённые partitions для группы проверяются до оптимизации. Unknown metadata не является
  доказательством независимости; исключение строки требует явной причины и отчёта, не улучшает молча баланс.
- Нулевая утечка по заданным связям проверяется точно. Целевой баланс — отдельная проектная функция:
  например, минимизация объявленного взвешенного отклонения от quotas, без просмотра качества моделей.
  [Документация scikit-learn](https://scikit-learn.org/stable/modules/cross_validation.html#cross-validation-iterators-for-grouped-data)
  подтверждает различие группировки и стратификации и предупреждает о неоптимальности greedy split;
  перенос Python-библиотеки в Kotlin-проект для этого не требуется.
- DSU занимает O((n+m)·α(n)); точное назначение c компонентов в k partitions — ограниченный перебор/branch-and-bound
  с худшей границей O(k^c) узлов, плюс стоимость проверки quotas. Лимит поиска ≠ доказанная невозможность:
  `Feasible`, `Optimal`, `Infeasible`, `SearchLimit`, `UnknownMetadata` различаются.
- Проверка: независимый полный перебор малых назначений, giant component, редкая сложная страта, временной
  конфликт, повторные trials, изменённый policy digest, детерминизм. Ни DSU, ни хороший баланс не доказывают
  отсутствие неизвестной answer leakage и не восстанавливают holdout после многократного использования.

**Владение/Deps:** предлагаемый P6.1.5, `eval` с узкими input records и P0.2.1 Digest при fingerprinting.
Модуль eval создан P6.1.4; P6.1.5 использует его готовые utilities. `core → eval` запрещена.
P6.1.4 и P6.1.5 заняты выполненными ядрами; не реализовывать повторно.
Runner, реальные frozen manifests, очистка answer-bearing artifacts, память warm/cold и tracking доступа
к holdout остаются у P6.1.1/P6.1.2. Ядро не закрывает FX-47 как runtime fixture.

### 8.4 OOO-08 — оптимальный порядок малого DAG при стоимости переключений

**Основание:** [routing §11.4](docs/operations/routing.md#sec-11-4), TODO P4.5.3, FX-45,
[RequirementGraph](core/src/main/kotlin/io/astrolabe/graph/RequirementGraph.kt).
Готовые Tarjan/Kahn/frontier не вычисляют минимальную стоимость последовательности с зависимостями;
их повторная реализация не является новым кандидатом.

**Ранний объём:** точный offline oracle для небольшого набора инкрементов и заданной матрицы стоимости
смены совместимых role/profile/prefix состояний. Ответ — порядок, его стоимость, основания и граница модели.

```text
DP[S ∪ {v}, v] = min_u (DP[S,u] + switchCost(u,v))
добавить v можно только если prerequisites(v) ⊆ S
```

Начальные стоимости задаются отдельно. Это адаптация subset-DP для sequencing; методическая база —
[авторская глава Waterloo о dynamic programming](https://www.math.uwaterloo.ca/~bico/papers/comp_chapterDP.pdf).
Для n узлов: O(n²·2^n) времени, O(n·2^n) памяти плюс восстановление пути; ресурсная граница задаётся явно.

- Оптимум только для объявленной pairwise cost model. Если стоимость зависит от TTL, нескольких ещё
  горячих prefix, всей истории, неполных billing или динамической latency, `(S,last)` недостаточно:
  либо отдельная расширенная постановка, либо Unknown/Unsupported, не ложная «экономия provider bill».
- Role/profile сами по себе не означают одинаковый prefix: нужны совместимость и версии `[S]/[R]`.
  Замороженные оценки сохраняют provenance; фиксированные charges не теряются при сравнении порядков.
- Все dependency edges и обязательные ограничения порядка сохраняются. Verified work не выполняется
  заново ради cache locality; semantic boundaries, обязательный review и latency limits нельзя сдвигать
  без разрешённой политики. При зависимостях вне среза нужно явное подтверждение их выполнения.
- Проверка: все топологические перестановки малых DAG, контрпример локальному greedy ordering,
  zero/equal/unknown costs, циклы, diamond, перестановки входов, округление Money и восстановление witness.
- При превышении размера допустим отдельно маркированный heuristic result либо отказ от exact oracle.
  Нельзя назвать greedy оптимумом; нельзя заявлять ускорение по одному synthetic графу.

**Владение/Deps:** предлагаемый P4.5.5, `core/route`; P2.1.1 graph, P0.3.3 Money/Profile, P1.8.1 Role
при потреблении роли — DONE. Реальный cost model, controller ordering hook и shadow campaigns остаются
P4.5.3/P2.2.2/P6. В runtime сначала достаточно простого hint; exact oracle нужен для измеримого сравнения
эвристик. Не создаётся live fan-out на несколько моделей и не удерживается нерелевантный контекст.

### 8.5 OOO-09 — точная аналитика вложенных и параллельных spans

**Основание:** [adapters §§15.2, 15.5](docs/platform/adapters.md#sec-15-5),
[evaluation §19.4](docs/evaluation/method.md#sec-19-4), TODO P1.11.1/P1.11.2.
`SpanId`, `Phase`, identities и Money уже есть; полного Span/Accounting store пока нет.

**Ранний объём:** чистая аналитика переданного trace snapshot: exclusive/inclusive cost, сумма worker
durations, union занятого времени, concurrency profile и длина известного causal critical path.
Проекция расчёта принадлежит ядру, runtime Span остаётся производителю P1.11.1.

```text
inclusiveCost(span) = exclusiveCost(span) + Σ inclusiveCost(children)
workerTime = Σ duration(exclusive work intervals)
busyTime = measure(union(exclusive work intervals))
longest[v] = duration(v) + max(longest[u]), u ∈ predecessors(v)
```

- Вложенность spans и causal dependency — разные графы. Critical path считают на явно переданных
  единицах выполнения/ожидания; inclusive parent duration не складывается с durations его детей.
- Для интервалов [0,6) и [4,10): worker time = 12, busy time = 10, max concurrency = 2. Из одного
  parent/child дерева нельзя доказать causal path = 10: отсутствующие wait/resource edges сохраняют
  неопределённость. Нельзя объявлять простую сумму wall-clock latency.
- Деньги учитываются один раз на producing span. Если exclusive amounts root=1, childA=2, childB=3,
  inclusive root=6; суммирование всех inclusive amounts повторно посчитало бы детей. Повторная доставка
  того же span ID дедуплицируется; конфликтующий payload, orphan/cycle и неизвестная стоимость видимы.
- Интервалы полуоткрытые, равные timestamps имеют зафиксированное правило; длительности неотрицательны.
  Один сопоставимый clock domain либо явное преобразование; skew и незавершённые spans не превращаются
  в нулевую длительность. Бюджетные числа/наносекунды защищены от overflow; currencies не смешиваются.
- Sweep line для union/concurrency — O(n log n); inclusive totals по дереву и longest path по DAG —
  O(V+E) до детерминированной сортировки. Реальный critical path требует полноты causal model; иначе
  выдаётся известная нижняя граница/Unknown, отдельно от наблюдаемого end-to-end elapsed time.
- Проверка: независимый перебор временных ячеек на малых целочисленных traces, полный перебор путей
  малого DAG, вложенные/соседние/перекрывающиеся интервалы, несколько roots, unknown money, cancel/open
  spans, повторные события и большие counters. Выход сохраняет provenance и все исключения.

**Владение/Deps:** предлагаемый P1.11.3, `core/telemetry`; P0.4.1 SpanId/Phase, P0.2.1 identities,
P0.3.3 Money — DONE. Span emission, timing capture, storage, invocation reconciliation и exports
остаются P1.11.1/P1.11.2. Ядро не притворяется готовой observability и не закрывает integration Done.

### 8.6 Что намеренно не попало в новый shortlist

- **Flaky → Bayesian classifier/SPRT:** нормативный P3.6.1 уже задаёт один изолированный rerun и
  `inconclusive` при расхождении. Другая статистическая политика — изменение требований, не ранний компонент.
- **Оптимизатор подмножества обязательных тестов:** экономия не разрешает выбрасывать обязательный acceptance.
  Полный scheduler/reuse proof требует реальных complete closures, а не просто set-cover по именам тестов.
- **Повторный Tarjan/Kahn, median или mean ratio:** уже реализованы P2.1.1/P2.6.5; новое название не создаёт задачу.
- **Baseline failure matching через Hungarian/min-cost flow:** текущий Baseline уже хранит namespace,
  signature и multiplicity. При точной идентичности достаточно multiset comparison; fuzzy matching
  рискует объявить новую ошибку старой. Новый requirement для сложного сопоставления не найден.
- **KB ranking с оптимизатором весов/embedding:** P4.1.3 прежде всего заданная линейная функция с eligibility
  и cap; обучение без размеченных данных не становится необходимым алгоритмом. Графовый отбор уже есть в OOO-01.
- **Автоматическое live-тюнингование α/k/R_max:** §16.3 разрешает offline настройку по данным, не per-turn
  выдуманную вероятность reuse. Break-even arithmetic полезна, но сама по себе проще пяти новых кандидатов.

**Проверка документа:** подтверждены DONE producers, отсутствие реализаций пяти ядер, свободные предложенные
канонические номера и 178 уникальных task ID. Новые IDs только предложены, не зарегистрированы. Внешние
первоисточники обосновывают методы, не требования ASTROLABE и не выигрыш выбранной политики. Изменения
ограничены документацией; повторный build не нужен. Сохранён возврат P0 validation → P1.8.2.

<a id="implementation-handoff"></a>

## 9. Передача реализации в новую сессию

### 9.1 Минимальные входы, размещение и проверяемые подшаги

Таблица дополняет карточки, не заменяет их Done. `Deps` ниже — подтверждённые producer-кандидаты;
окончательный список выводится из фактического API и импортов. Чистые input projections объявляются
за новым ядром, отсутствующие runtime-типы не имитируются. A/B/C — подшаги **одной** канонической задачи;
они не увеличивают число задач и не разрешают ставить DONE по частичному результату.

| ID | Модуль / пакет | Готовые producers для проверки | Порядок внутри ядра |
|---|---|---|---|
| OOO-02 | `eval` / `io.astrolabe.eval` | P0.1.1 build convention; P0.3.3 Money/BillableUsage; P0.2.1 при использовании IDs/Digest | A: trial/policy + arithmetic/eligibility; B: pairing/clusters + uncertainty; C: составной verdict + simulation validation; подробности §9.2 |
| OOO-05 | `core` / `workspace` | P1.1.1 Scope; P1.7.8 PathPattern; P1.2.6 при потреблении WorkspacePath; P0.2.1 при namespaces | A: доказать необходимость open-world scopes, зафиксировать grammar; B: automata/intersection/difference/witness; C: compatibility oracle, exhaustion→Unknown, full checks |
| OOO-07 | `eval` / `io.astrolabe.eval` | P0.1.1; P0.2.1 при policy digest/IDs; собственные workload records | A: policy/metadata + must-link components и validator; B: constrained assignment; C: exhaustive oracle, infeasibility/search-limit certificates, determinism |
| OOO-08 | `core` / `route` | P2.1.1 graph/Increment; P0.3.3 Money/Profile; P1.8.1 при Role | A: зафиксировать Markov/pairwise cost boundary; B: subset-DP + predecessor reconstruction; C: all-topological-orders oracle, resource limits, неподдерживаемая история стоимости |
| OOO-06 | `core` / `route` | P0.3.3 Money/Profile; P0.2.2 Budget; P0.1.3 при Tier/Config | A: конечные states/transitions и terminal semantics; B: expected cost/acceptance; C: path-enumeration oracle, unknowns, floors/affordability отделены от ожидания |
| OOO-09 | `core` / `telemetry` | P0.4.1 SpanId/Phase; P0.2.1 identities; P0.3.3 Money | A: snapshot identity/dedup/clock domain; B: cost tree + interval sweep; C: explicit causal DAG/critical-path limits + independent oracles |
| OOO-04 | `core` / `atlas` | P0.2.1 identities; P0.1.3 Defaults; P1.3.1 Atlas/P1.3.3 SymbolIndex при использовании; P1.4.2 Closure при использовании | A: graph/hunks/anchors/completeness contract; B: reachability/test joins/risk; C: conservative fallback, independent oracle и вычислительные случаи FX-37/54 |

Проверенные места существующих контрактов, которые часто путают:

- `provider-api/src/main/kotlin/io/astrolabe/provider/{Usage,Capabilities}.kt` — Money, BillableUsage, Profile.
- `core/src/main/kotlin/io/astrolabe/evidence/Receipt.kt` — **Closure**, TestedInputs, Receipt; не `verify/Closure.kt`.
- `core/src/main/kotlin/io/astrolabe/verify/Check.kt` — Applicability enum; `verify/Scheduler.kt` — текущая currency.
- `core/src/main/kotlin/io/astrolabe/workspace/PathPattern.kt` — producer **P1.7.8**; `WorkspacePath.kt` — **P1.2.6**.
- `core/src/main/kotlin/io/astrolabe/graph/{Planning,RequirementGraph,Dependencies}.kt` — Sizing и графовые алгоритмы;
  `Dependencies.kt` содержит internal функции: учитывать доступность, особенно из отдельного `eval` модуля.
- `core/src/main/kotlin/io/astrolabe/event/AgentEvent.kt` — SpanId/Phase, но не готовая telemetry Span реализация.

Перед выделением ещё одного utility проверь существующую реализацию. Потребление public record не требует
переноса его владения. Изменение готового API требует проверки прежних потребителей и отдельной записи причины.
Применяются `.editorconfig`, TODO §2.3, explicit public API, immutable/defensive collections и deterministic policy.
Результат различает invalid input, unknown data, отказ по вместимости и исчерпание вычислительного лимита.

### 9.2 Точный старт первой задачи — OOO-02 / предполагаемая P6.1.4

**Исторический план выполненного ядра:** P6.1.4 DONE, D-57. Не запускать повторно;
этот раздел сохраняет принятые критерии A/B/C и служит образцом. Актуальная очередь — §2.

1. **Допуск:** проверить отсутствие ACTIVE/частичной реализации и свободные P6.1.4/D-57. Прочитать карточку
   OOO-02, `docs/evaluation/method.md` §§19.2/19.4/19.6, `docs/economics/costs.md`, D-28/D-48 и `Usage.kt`.
   Создать `audit/OUT-OF-ORDER-P6.1.4.md`, зарегистрировать задачу/типы/явные Deps и ACTIVE-карточку по §4/§6.
   Отсутствующие runner/Accounting/frozen campaigns относятся к родителю; расчёт получает явные записи.
2. **Минимальный eval:** только после регистрации добавить `:eval` в существующий `settings.gradle.kts`;
   применить `astrolabe.kotlin-library` в `eval/build.gradle.kts`. Следовать текущему convention plugin и catalog;
   не менять версии Kotlin/Gradle/JDK ради задачи. Зависимость направлена `eval → core`, а не наоборот.
   `api(project(":core"))` нужна, если public API раскрывает core/provider типы; test fixtures добавляются
   только при реальном использовании. Runner entry point, live adapters и campaign store здесь не создаются.
3. **Заморозить проект оценки до результатов:** в журнале и версионированной policy описать estimand,
   trial/pair/cluster keys, stratum weights, численную точность, confidence, one-sided method, small-sample
   policy, missing pairs, multiple-selection, PRNG/seed/replicates и минимально достаточные данные.
   Параметры, не заданные спецификацией, — новый D-nn с обоснованием, а не «существующие defaults».
   Первичные источники выбранного статистического метода нужно прочитать до его реализации; метод разрешено
   выбрать автономно в пределах требований, не подгоняя его после просмотра удачного verdict.
4. **A — арифметика и eligibility:** написать минимальные падающие ручные таблицы, затем Q/E/score,
   cost-per-accepted, one-off repayment и набор причин ineligible/unknown. Учитывать все внутренние попытки
   в одном trial. Unknown billing, ноль accepted, неподдерживаемая валюта, невалидные веса и потеря сложной
   страты остаются явными. Успех A фиксируется как checkpoint IN_PROGRESS всей P6.1.4.
5. **B — парная неопределённость:** сначала сверить ключи/полноту пар и frozen metadata. Повторы внутри
   task/repository не создают независимые единицы. Реализовать выбранную границу с отдельным результатом
   для недостаточных/вырожденных данных. Margin **−0.02** относится к разности долей candidate−baseline,
   а не к Q в шкале 0–100; нижняя граница должна быть **строго выше** margin отдельно overall и complex.
6. **C — verdict и проверка метода:** свести статистические условия с остальными quality floors,
   invariant checks, ceilings, economic policy и evaluation-integrity evidence. Высокий общий score или
   одна прошедшая граница не перекрывают остальные условия. Inconclusive сохраняет baseline. Отсутствие
   реальных frozen/integrity evidence не позволяет выдать разрешение на adoption по synthetic данным.
7. **Эксперименты:** заранее записать модели генерации, seeds, число повторов и критерии принятия метода.
   Покрыть null/границу −2 pp/вред/улучшение, внутрикластерную корреляцию, неравные размеры кластеров,
   редкую complex-страту, недостающие пары, вырожденную выборку и multiple-selection. Проверять coverage
   и false-promotion rate с собственной Monte Carlo uncertainty; не только совпадение двух реализаций.
   Симуляционный провал ведёт к исправлению метода/контракта, а не к ослаблению порога или выбору другого seed.
8. **Финиш:** focused tests → review требований/математики/границ → отдельный `:eval:updateKotlinAbi` →
   полный `build` по §5 → checkpoint по §6. P6.1.4 DONE только после A+B+C и проверки метода;
   P6.1.3, P6.1.1/P6.1.2 и live promotion остаются в своих статусах.

Если задача переносится в другую сессию, сохраняются достигнутый подшаг, версия policy, input digest,
точная команда и нерешённый контрпример. Повторный запуск не начинает новую P6.1.4 и не выбирает другой
метод лишь потому, что прежний дал неудобный результат.

### 9.3 Проверка checkpoint перед сохранением

Код и его ABI/tests лежат в выбранном модуле; журнал — `audit/OUT-OF-ORDER-<фактический-ID>.md`.
При каждом завершённом подшаге обновить все четыре места из §6: TODO, actual_state, CONTINUE-TASK,
журнал/индекс этого документа. У промежуточного checkpoint остаются ACTIVE/IN_PROGRESS и точное next action.

- Сохранить сводку test XML **до** следующего filtered run: выполненные tests/failures/errors/skips,
  модуль и платформу; UP-TO-DATE отделять от нового исполнения. Новый `eval` имеет собственный отчёт.
- Проверить `git diff --check`, весь относящийся к задаче diff, API dump, неизменность прежнего acceptance,
  отсутствие случайных suppressions/отключённых тестов и корректность всех локальных ссылок.
- Пересчитать реальные заголовки `#### Pn.m.k ... · STATUS`, проверить уникальность IDs и producer mapping.
  Числа из §4 — пример перехода, не текст для слепого копирования. PROPOSED в этом файле не задача TODO.
- При DONE у ядра сохранить родителю конкретный перечень оставшейся интеграции. Обновить §7 по факту;
  из очереди §2 убрать выполненный ID, затем обновить первый ID в шапке, §1 и обоих handoff-файлах.
  Карточку и стартовый пример выполненной задачи оставить с явной отметкой DONE/исторический контекст,
  чтобы ни пример OOO-02 в §9.2, ни старая команда тестов не запускали завершённое ядро повторно.
- Коммит включает только свои код, тесты, ABI и state/docs. После коммита записать/проверить `git status`
  и сообщить hash; запись «коммит, содержащий этот checkpoint» допустима вместо самоссылочного hash внутри него.
  Push остаётся отдельным действием. При красном build сначала исправить или сохранить честный IN_PROGRESS.
- Обычную очередь P0 validation → P1.8.2 восстановить после завершения разрешённого объёма. Продолжение
  нескольких ядер требует соответствующего поручения; переход к следующему снова начинается с допуска §4.

### 9.4 Готовое поручение для новой сессии

Следующий текст запускает несколько задач, если пользователь его поручит. Само чтение документа
и текущее поручение отсортировать список реализацию не активируют.

```text
Продолжи out-of-order реализацию по OUT-OF-ORDER-PROPOSAL-TASKS.md.
Сначала проверь TODO §1.2: продолжи ACTIVE или сохранённое незавершённое ядро с его точного checkpoint.
Если такого нет, последовательно выполняй допустимые невыполненные ядра по сложности из §2,
начиная с OOO-07 (предполагаемая P6.1.5), по одному компоненту за раз; P6.1.4/P5.1.5 уже DONE.
Соблюдай допуск, границы Done, проверки и обновление документов из §4–§6/§9.
Не закрывай родителя по готовности ядра. После каждого компонента сохраняй проверенный коммит;
при переносе работы оставляй точный checkpoint и действующий канонический ID.
После завершения порученного объёма восстанови основной resume point P0 validation → P1.8.2.
```

**Проверка этой подготовки на `f6a4feb`:** порядок §2 совпадает с семью PROPOSED-строками §7;
178 task IDs и все их статусы совпадают с исходным TODO, семь предлагаемых номеров и D-57 свободны.
Проверены локальные ссылки и anchors этого документа, согласованность handoff и `git diff --check`.
Изменена только документация, build/tests не повторялись; новые ACTIVE/DONE записи не создавались.
